<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adr.zombie">

    <!-- Cho phép internet (QC, analytics, sau này online) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Cho WebView + asset local -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Khai báo OpenGL ES -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:label="Anger Of Stick: Zombie"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        <!-- QUAN TRỌNG NHẤT: bật tăng tốc -->
        android:hardwareAccelerated="true"

        <!-- Cho WebView load file:/// -->
        android:usesCleartextTraffic="true"

        <!-- Fullscreen, không action bar -->
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

        <activity
            android:name=".MainActivity"
            android:exported="true"

            <!-- Game xoay ngang -->
            android:screenOrientation="landscape"

            <!-- Không restart khi xoay / resize -->
            android:configChanges="orientation|screenSize|keyboardHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
